[project]
name = "radcod"
version = "0.1.0"
description = "AI Software Engineer - Devin-like autonomous agent"
dependencies = [
    "openhands-sdk",
    "openhands-tools",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "black",
    "ruff",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
